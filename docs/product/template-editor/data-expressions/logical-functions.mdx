---
title: Logical Functions
sidebar_position: 8
description: "Use Eledo Logical Functions to combine boolean values and evaluate conditions with NOT, AND, and OR in template expressions."
keywords:
  - Eledo Logical Functions
  - boolean expressions
  - NOT function
  - AND function
  - OR function
  - template conditions
  - template expressions
---

import DocFeedbackForm from '@site/src/components/DocFeedbackForm';

# Logical Functions

Logical functions allow you to combine and evaluate boolean values.

---

## NOT(A)

Returns the logical negation of `A`.

```
NOT( checked )
```

---

## AND(A, B, ...)

Returns `true` if all provided values are `true`.

```
AND( check1, check2, check3 )
```

---

## OR(A, B, ...)

Returns `true` if at least one provided value is `true`.

```
OR( check1, check2, check3 )
```

---

<DocFeedbackForm />