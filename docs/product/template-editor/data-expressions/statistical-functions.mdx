---
title: Statistical Functions
sidebar_position: 9
description: "Use Eledo Statistical Functions to aggregate array values and calculate sums from repeated data in template expressions."
keywords:
  - Eledo Statistical Functions
  - SUM function
  - SUMC function
  - array aggregation
  - repeated data
  - template expressions
---

import DocFeedbackForm from '@site/src/components/DocFeedbackForm';

# Statistical Functions

Statistical functions allow you to aggregate values from arrays.

---

## SUM(V, X)

Returns the sum of expression `X` evaluated for each item in array `V`.

```
SUM( items, line_total )
```

---

## SUMC(V, X, F, T)

Returns the sum of expression `X` for items in array `V` where field `F` equals value `T`.

```
SUMC( items, line_total, type, "service" )
```

---

<DocFeedbackForm />