---
title: Populating Dropdown
sidebar_position: 7
description: "Learn how to populate PDF form dropdown fields with monday.com values and map them to valid options using the SWITCH function."
keywords:
  - populate dropdown field
  - Eledo PDF forms
  - monday.com status
  - dropdown options
  - SWITCH function
  - value mapping
  - PDF form fields
---

import DocFeedbackForm from '@site/src/components/DocFeedbackForm';

# Populating Dropdown

To populate a Dropdown field in a PDF form using data from your monday.com board, follow these steps:

- In the Eledo editor, select the dropdown form field  
- Click into **Data (expression)** to open the expression builder  
- Select a value (for example: `item.status.label`)  
- Confirm and apply the value  

![image](/img/integrations/monday/MondayPDFFormDropdownFormField.gif)

---

## Value Mapping

Dropdown fields require an exact match between the value and one of the available options.

If the values do not match, use the `SWITCH()` function to map them.

Example:

```
SWITCH(
    item.status.label,
    "Mon", "Monday",
    "Tue", "Tuesday",
    "Wed", "Wednesday",
    "Thu", "Thursday",
    "Fri", "Friday"
)
```

---

<DocFeedbackForm />